# ====================================
# User Profiling System - Environment Configuration
# ====================================
# ⚠️ SECURITY WARNING:
# 1. Copy this file to .env and modify ALL placeholder values
# 2. NEVER commit .env file to version control!
# 3. Use strong passwords (min 16 chars, mixed case, numbers, symbols)
# 4. Generate JWT secret: openssl rand -base64 32
# ====================================

# ====================================
# Database Credentials
# ====================================

# MySQL Configuration
MYSQL_ROOT_PASSWORD=CHANGE_THIS_MYSQL_ROOT_PASSWORD_IN_PRODUCTION
MYSQL_USER=userservice
MYSQL_PASSWORD=CHANGE_THIS_MYSQL_PASSWORD_IN_PRODUCTION

# MongoDB Configuration
MONGO_USERNAME=admin
MONGO_PASSWORD=CHANGE_THIS_MONGO_PASSWORD_IN_PRODUCTION

# Redis Configuration
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD_IN_PRODUCTION

# ====================================
# Application Configuration
# ====================================

# JWT Secret (REQUIRED, minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_JWT_SECRET_KEY_AT_LEAST_32_CHARACTERS_REQUIRED

# Admin User Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ====================================
# Service Ports (Optional Override)
# ====================================

# GATEWAY_PORT=8080
# USER_SERVICE_PORT=8081
# PROFILE_SERVICE_PORT=8082
# BEHAVIOR_SERVICE_PORT=8083
# TAG_SERVICE_PORT=8084

# ====================================
# Infrastructure Ports (Optional Override)
# ====================================

# CONSUL_PORT=8500
# REDIS_PORT=6379
# MONGODB_PORT=27017
# MYSQL_PORT=3306

# ====================================
# Production Recommendations
# ====================================
# 1. Use strong passwords (min 16 characters, mixed case, numbers, symbols)
# 2. Generate unique JWT secret: openssl rand -base64 32
# 3. Enable SSL/TLS for all database connections in production
# 4. Rotate passwords regularly
# 5. Use secrets management tools (Vault, AWS Secrets Manager, etc.)
